package me.pollos.polloshook.impl.module.misc.announcer.modes;

public enum AnnouncerAction {
   PLACE,
   BREAK,
   EAT,
   JOIN,
   LEAVE,
   ATTACK,
   KILL,
   WALK;

   // $FF: synthetic method
   private static AnnouncerAction[] $values() {
      return new AnnouncerAction[]{PLACE, BREAK, EAT, JOIN, LEAVE, ATTACK, KILL, WALK};
   }
}
