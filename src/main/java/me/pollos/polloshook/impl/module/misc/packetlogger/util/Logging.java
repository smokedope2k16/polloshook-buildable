package me.pollos.polloshook.impl.module.misc.packetlogger.util;

public enum Logging {
   FILE,
   CHAT,
   BOTH;

   // $FF: synthetic method
   private static Logging[] $values() {
      return new Logging[]{FILE, CHAT, BOTH};
   }
}
