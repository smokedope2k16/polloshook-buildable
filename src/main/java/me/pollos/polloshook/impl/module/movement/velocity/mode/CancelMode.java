package me.pollos.polloshook.impl.module.movement.velocity.mode;

public enum CancelMode {
   EXPLOSIONS,
   VELOCITY,
   BOTH;

   // $FF: synthetic method
   private static CancelMode[] $values() {
      return new CancelMode[]{EXPLOSIONS, VELOCITY, BOTH};
   }
}
