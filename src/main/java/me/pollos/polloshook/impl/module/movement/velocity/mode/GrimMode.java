package me.pollos.polloshook.impl.module.movement.velocity.mode;

public enum GrimMode {
   LAST,
   CURRENT_SERVER,
   CURRENT_ROTATIONS,
   CURRENT;

   // $FF: synthetic method
   private static GrimMode[] $values() {
      return new GrimMode[]{LAST, CURRENT_SERVER, CURRENT_ROTATIONS, CURRENT};
   }
}
