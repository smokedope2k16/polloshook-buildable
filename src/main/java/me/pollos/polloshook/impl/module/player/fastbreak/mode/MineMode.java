package me.pollos.polloshook.impl.module.player.fastbreak.mode;

public enum MineMode {
   PACKET,
   INSTANT;

   // $FF: synthetic method
   private static MineMode[] $values() {
      return new MineMode[]{PACKET, INSTANT};
   }
}
