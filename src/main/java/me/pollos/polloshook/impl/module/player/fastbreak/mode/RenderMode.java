package me.pollos.polloshook.impl.module.player.fastbreak.mode;

public enum RenderMode {
   STATIC,
   GROW,
   SHRINK;

   // $FF: synthetic method
   private static RenderMode[] $values() {
      return new RenderMode[]{STATIC, GROW, SHRINK};
   }
}
