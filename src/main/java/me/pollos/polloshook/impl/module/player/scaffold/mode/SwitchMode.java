package me.pollos.polloshook.impl.module.player.scaffold.mode;

public enum SwitchMode {
   HOLD,
   SILENT,
   NONE;

   // $FF: synthetic method
   private static SwitchMode[] $values() {
      return new SwitchMode[]{HOLD, SILENT, NONE};
   }
}
