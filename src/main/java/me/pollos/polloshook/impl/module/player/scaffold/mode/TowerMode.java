package me.pollos.polloshook.impl.module.player.scaffold.mode;

public enum TowerMode {
   CLASSIC,
   JUMP,
   HOP,
   OFF;

   // $FF: synthetic method
   private static TowerMode[] $values() {
      return new TowerMode[]{CLASSIC, JUMP, HOP, OFF};
   }
}
