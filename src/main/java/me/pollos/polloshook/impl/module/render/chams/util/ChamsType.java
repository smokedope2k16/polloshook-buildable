package me.pollos.polloshook.impl.module.render.chams.util;

public enum ChamsType {
   FILL,
   WIRE_FRAME,
   BOTH,
   OFF;

   // $FF: synthetic method
   private static ChamsType[] $values() {
      return new ChamsType[]{FILL, WIRE_FRAME, BOTH, OFF};
   }
}
