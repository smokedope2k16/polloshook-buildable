package me.pollos.polloshook.impl.module.render.customsky.mode;

public enum CustomSkyMode {
   ONLY_SKY,
   ONLY_FOG,
   BOTH,
   OFF;

   // $FF: synthetic method
   private static CustomSkyMode[] $values() {
      return new CustomSkyMode[]{ONLY_SKY, ONLY_FOG, BOTH, OFF};
   }
}
