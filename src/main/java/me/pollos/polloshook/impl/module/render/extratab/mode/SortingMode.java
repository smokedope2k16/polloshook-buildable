package me.pollos.polloshook.impl.module.render.extratab.mode;

public enum SortingMode {
   VANILLA,
   PING,
   FRIENDS,
   IRC,
   ABC,
   LENGTH;

   // $FF: synthetic method
   private static SortingMode[] $values() {
      return new SortingMode[]{VANILLA, PING, FRIENDS, IRC, ABC, LENGTH};
   }
}
