package me.pollos.polloshook.impl.module.render.worldeditesp.mode;

public enum RenderMode {
   FILL,
   OUTLINE,
   BOTH;

   // $FF: synthetic method
   private static RenderMode[] $values() {
      return new RenderMode[]{FILL, OUTLINE, BOTH};
   }
}
